REPLACE ONLY the existing function:

function Wo_RegisterFollow(id, is_confirm,show_modal= false) { ... }

inside:
streams/themes/sunshine/layout/extra_js/content.phtml

with the following complete function.

This version is deliberately server-authoritative:
- it never renders Requested;
- it does not pass confirm_followers;
- it does not decide the final state before the AJAX response;
- it renders Following only after the server returns state=following;
- it renders Follow only after the server returns state=follow;
- it leaves the button unchanged while the request is in flight;
- it logs useful browser-side diagnostics to the console.
